---
# Target env: `vagrant` or `aws`
target:  "vagrant"
# The root
faas_root_dir: "/faas"
# Branch, or version SHA of the faas repository
github_repo_version: "master"
data_dir: "/data"
share_dir: "/share"

